import { ValidatePermissionDto } from './validate-permission.dto';

describe('ValidatePermissionDto', () => {
  it('should be defined', () => {
    expect(new ValidatePermissionDto()).toBeDefined();
  });
});
