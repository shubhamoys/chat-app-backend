import { AppResponseInterceptor } from './app-response.interceptor';

describe('AppResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new AppResponseInterceptor()).toBeDefined();
  });
});
